order: 2
slug: inference
title: The Inference Engineering Book
subtitle: vLLM and SGLang
blurb: >
  How LLM serving systems actually work, read out of the vLLM and SGLang source.
  Continuous batching, PagedAttention and RadixAttention, quantization,
  parallelism, speculative decoding, and how to benchmark any of it honestly.
level: Intermediate to advanced
module_count: 14

# This course ships as finished HTML rather than Markdown: no Markdown sources
# exist for it, and converting 3,300+ code blocks of dense systems material
# would risk corrupting content that is currently correct. The build copies the
# pages verbatim and re-themes them through CSS custom properties instead.
type: prebuilt
source: html
entry: index.html
